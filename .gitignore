node_modules/
dist/
.env
.env.local
*.log

# Local data (memory, eval fixtures, runtime metrics output)
# Anchored to the root so src/metrics/ (source code) stays tracked.
/data/
/metrics/

# OS
.DS_Store
public/events.jsonl
